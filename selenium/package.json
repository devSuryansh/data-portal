{
  "name": "portal-selenium",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "mocha \"src/tests/**/*.spec.js\" --timeout 120000",
    "test:headless": "HEADLESS=true mocha \"src/tests/**/*.spec.js\" --timeout 120000",
    "test:version": "mocha \"src/tests/versionFooter.spec.js\" --timeout 120000",
    "test:dictionary": "mocha \"src/tests/dictionary.spec.js\" --timeout 120000"
  },
  "devDependencies": {
    "chai": "^4.4.1",
    "dotenv": "^16.4.5",
    "mocha": "^10.7.3",
    "selenium-webdriver": "^4.25.0"
  }
}
